# TOD-002 待办跳转源笔记并锚定（低强调）测试用例

## 用例1：ICON呈现
- 条件：TODO 含 `metadata.originNoteId+lineHash`
- 期望：卡片右上角按钮行最前显示小 ICON（低强调，尺寸16，随 `group-hover` 渐显）
- 执行：进入“待办”视图；检查按钮行最前是否显示 `mdi:link-variant`
- 结果：ICON展示符合规范

## 用例2：跳转与锚定
- 步骤：点击 ICON，打开 `/detail?id=&anchor=` 新窗口
- 期望：详情页滚动居中并高亮该任务行约 1.5 秒
- 执行：直接访问 `detail?id=<originNoteId>&anchor=<lineHash>`；截图 `detail-anchored-*.png`
- 结果：页面加载成功，锚点跳转与高亮正常

## 用例3：无来源数据不显示
- 条件：缺失 `originNoteId` 或 `lineHash`
- 期望：不显示 ICON
- 执行：构造无来源 TODO；检查按钮行最前不出现跳转 ICON
- 结果：不显示 ICON
